POSTGRES_PRISMA_URL="postgresql://user:password@host:5432/dbname?schema=public"
POSTGRES_URL_NON_POOLING="postgresql://user:password@host:5432/dbname?schema=public"
JWT_SECRET="a long random string"
SEED_ADMIN_EMAIL="admin@example.com"
SEED_ADMIN_PASSWORD="set a real password before running seed in any shared environment"

# Only needed to run `npm run migrate:from-v1` (Phase 4) — points at V1's
# own Postgres database (read-only access is enough; the script never
# writes back to it).
V1_DATABASE_URL="postgresql://user:password@host:5432/yahshua_agreements?schema=public"
